var ScriptLoader = require('./ScriptLoader');
var Logger = require('./Logger');

var scriptLoader = new ScriptLoader();


module.exports = {
    API: {
        loadModule: function(name) {
            return scriptLoader.loadModule(name);
        }
    },
    Logger: Logger
}